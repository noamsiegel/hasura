kind: Connector
version: v2
definition:
  name: weather
  subgraph: app
  source: hasura/go:v1.8.0
  context: .
  envMapping:
    HASURA_SERVICE_TOKEN_SECRET:
      fromEnv: APP_WEATHER_HASURA_SERVICE_TOKEN_SECRET
    MUTATION_CONCURRENCY_LIMIT:
      fromEnv: APP_WEATHER_MUTATION_CONCURRENCY_LIMIT
    OTEL_EXPORTER_OTLP_ENDPOINT:
      fromEnv: APP_WEATHER_OTEL_EXPORTER_OTLP_ENDPOINT
    OTEL_SERVICE_NAME:
      fromEnv: APP_WEATHER_OTEL_SERVICE_NAME
    QUERY_CONCURRENCY_LIMIT:
      fromEnv: APP_WEATHER_QUERY_CONCURRENCY_LIMIT
